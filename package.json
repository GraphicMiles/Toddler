{
  "name": "forgeai",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:mobile": "vite --host",
    "build": "vite build",
    "lint": "oxlint .",
    "preview": "vite preview",
    "android:init": "npx cap init ForgeAI ai.forgeai.app --web-dir=dist",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "android:build": "npm run llama:bootstrap && npm run android:preflight && npm run build && npx cap sync android && cd android && ./gradlew assembleDebug --no-daemon --no-parallel --max-workers=2",
    "test": "node test/deviceCapacity.test.js && node test/onDeviceCapability.test.js && node test/modelProvider.test.js && node test/toolRegistry.test.js && node test/safety.test.js && node test/agentCore.test.js && node test/workspaceProvider.test.js && node test/workspacePolicy.test.js && node test/virtualWorkspace.test.js && node test/rag.test.js && node test/modelCatalog.test.js && node test/promptProfiles.test.js && node test/safePath.test.js && node test/modelManifest.test.js",
    "llama:bootstrap": "bash scripts/bootstrap-llama-cpp.sh",
    "android:preflight": "bash scripts/verify-android-environment.sh"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.8",
    "@capacitor/app": "^7.1.2",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/filesystem": "^7.1.8",
    "@capacitor/haptics": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.7",
    "@capacitor/status-bar": "^7.0.0",
    "@forgeai/device-capacity": "file:plugins/device-capacity",
    "framer-motion": "^11.0.0",
    "lucide-react": "^0.475.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "oxlint": "1.76.0",
    "vite": "^6.0.0"
  }
}
