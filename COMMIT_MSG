fix: resolve remaining audit issues — fake data, dead tools, depth levels, dry run

1. Social media scraping no longer returns fake data
   - scrapePublicPosts() now uses real web research instead of
     fabricating posts with user0, user1 placeholders
   - Honest error fallback instead of made-up content

2. Research depth levels now actually do something
   - STANDARD: 5 results, higher relevance threshold
   - COMPREHENSIVE: 10 results, lower threshold
   - RAW: 8 results, no filtering
   - Depth info passed to native provider

3. Archive mode / fetch_page wired into agentic loop
   - New fetch_page tool schema added to toolSchemas.js
   - Agentic loop can now fetch full page content after search
   - Falls back to CORS fetch in browser mode

4. fs:analyze upgraded from trivial counts to full analysis
   - File type classification (source/config/style/test/docs)
   - Entry point detection (index, main, app, server)
   - Key file identification (package.json, tsconfig, etc.)
   - Reads package.json for project name/version/scripts
   - Extension breakdown sorted by frequency

5. Dry run mode checked in fullAutonomyRunner
   - GitHub API actions now check githubAutomation.dryRun
   - Returns dry-run status instead of executing when enabled

6. research:query and research:scrape remain registered
   - Now also accessible via agentic loop as search_web + fetch_page
   - Dead code issue addressed by wiring into the new tool flow

Lint: 0 warnings, 0 errors. Tests: all 25 suites passing.
